import React, { useState } from "react";
import { StateManager } from './StateManager';

/*
  TODO:
    General Styling (Fri)
      Learn api 
    Dark Mode & Clean code up & Documentation (Sat)
      Learn api 
    Make all buttons work with api calls (Sunday)
    Polish and further documentation (Monday)
    Upload to server and test (Tuesday)
*/

function App() {
  return (
    <div className="App">
      { <StateManager/> }
    </div>
  );
}

export default App;
